/** Theme preference stored for the shell layout. */
export type ThemeMode = "light" | "dark" | "system";

/** Palette preference used by the UI theme provider. */
export type PaletteMode = "default" | "warm" | "cool" | "mint";
