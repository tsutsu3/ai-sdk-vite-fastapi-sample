steps:
  - name: gcr.io/kaniko-project/executor:latest
    args:
      - --dockerfile=Dockerfile
      - --context=dir://.
      - --destination=${_IMAGE}
      - --cache=true
      - --cache-repo=${_CACHE_IMAGE}
      - --cache-ttl=168h
substitutions:
  _IMAGE: ""
  _CACHE_IMAGE: ""
